# 更新日志

[English](CHANGELOG.md) | **简体中文**

面向玩家的版本更新历史如下；每个版本的完整发布说明与平台文案位于
[`docs/releases/`](docs/releases/index.md)。

## 1.1.0

**发布日期：2026-07-25**

### 命令方块检查

- 带方块实体数据的命令方块物品会在悬停提示中按 Studio 规则换行显示命令开头。
- 物品或 3D 投影携带共享注释时，注释会紧跟标题显示。
- 物品携带 Studio 编辑历史时，同时显示最后编辑时间和编辑者。
- 新增可自定义工作模式快捷键，默认 `Alt+Tab`。
- 准星指向命令方块时，可在方块前方显示面向玩家的命令全息面板；投影采用 Studio 同款结构化换行，并锁定首次命中的方块表面。
- 创造模式“工具与实用物品”分类新增命令方块扫描仪，手持即可临时启用投影；扫描仪基于原版望远镜物品栈，不增加双端硬依赖。
- 修复长命令空格宽度、自动换行、鼠标命中与光标位置错位。
- 补全列表、命令用法和快速文档会避让参数状态栏，并在空间不足时翻转到当前行上方。

### 聊天命令助手

- 聊天输入以 `/` 开头时，在左下角打开 Studio 多行命令编辑器，并在右侧四分之一屏幕保留命令助手。
- 命令补全列表改由 Studio 编辑器内部定位，不再滞留在原版聊天框底部。
- 右侧文档面板可见时不再显示编辑区悬停文档，并修复点击补全后瞬时解析状态导致的崩溃。
- 补全弹层提升到编辑器正文上方，带图标候选的图标槽不再遮挡光标前文字。
- 单击引号内文本时会选中引号内的完整内容，第二次单击会把光标移到内容末尾；接受补全时会替换整个选区，不再与幽灵文本重叠。
- 复用 Studio 的 Brigadier 补全、快速文档和 `Ctrl+Space` 主动补全。
- 修复替换原版输入框后多行聊天编辑器未立即取得键盘焦点的问题。
- 支持 `Enter` 发送与 `Shift+Enter` 手动换行。
- 支持方块、物品、粒子和玩家可视化候选。
- 粒子预览支持动画；在线玩家候选可显示头像、延迟与游戏模式。
- 普通聊天不会打开命令助手。

### 运行与兼容

- 新增 Minecraft `1.20.4` / NeoForge `20.4.167` 原生分支，使用 Java 17 与该版本官方 NeoGradle 工具链。
- 1.21.4 与 26.2 的最低 NeoForge 构建下调，便于在采用较早加载器的小版本整合包中安装。
- 修复 Minecraft 1.21.4 与 26.2 中因纹理绘制参数顺序差异导致 Studio 图标被放大和裁切的问题。
- 新增运行测试按钮，保存草稿后让命令方块执行一次，无需临时红石。
- 命令预览、运行测试等网络能力改为逐载荷协商。
- 客户端和服务端模组版本可以不同。
- 不支持的共享功能会隐藏或禁用，而不是中断连接。
- 继续支持仅客户端或仅服务端安装。

[查看 1.1.0 完整发布说明](docs/releases/1.1.0/release-notes.md)

## 1.0.0

**发布日期：2026-07-21**

### NeoForge 原生移植

- 面向 Minecraft `1.21.1` 与 NeoForge `21.1.217` 重建项目。
- 保留 NeoForge 官方 MDK、ModDevGradle、资源模板和运行配置结构。
- 生命周期、按键、配置页面和客户端事件改为 NeoForge 原生 API。
- 不依赖 Fabric API、Architectury、Sinytra Connector 或其他兼容层。

### Studio 编辑器

- 将原版单行界面升级为多行命令工作台。
- 新增行号、语法着色、当前行与错误行标记、搜索替换和精确错误跳转。
- 新增 Brigadier 补全、幽灵文本、参数提示、命令模板和占位符跳转。
- 新增 Component、物品数据组件、方块、物品、附魔、玩家和粒子辅助。
- 新增工作区页签、连锁命令方块分组、独立固定与横向滚动。
- 新增快速文档、问题与输出面板、共享注释和十次编辑历史回滚。
- 新增自动保存、未保存退出确认、响应式缩放与中英文提示。

### 模组命令软兼容

- 为 WorldEdit、Carpet、Create、CMDCam、Corpse、Dynamic Trees、Exposure、Serene Seasons、Supplementaries、WaterFrames、SecurityCraft 和 Yes Steve Model 提供已确认的根命令说明。
- 未内置的服务器命令继续根据当前 Brigadier 命令树生成候选和参数类型说明。

[查看 1.0.0 完整发布说明](docs/releases/1.0.0/release-notes.md)
